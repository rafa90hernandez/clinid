services:
  db:
    image: postgres:16
    container_name: clinid_db
    environment:
      POSTGRES_USER: clinid
      POSTGRES_PASSWORD: clinid
      POSTGRES_DB: clinid
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    container_name: clinid_api
    depends_on:
      - db
    ports:
      - "3001:3001"
    environment:
      DATABASE_URL: postgres://clinid:clinid@db:5432/clinid
      JWT_SECRET: change-me
      PORT: "3001"
      NODE_ENV: development
    volumes:
      - ./api:/usr/src/app
      - /usr/src/app/node_modules
    command: sh -c "npm install && npx prisma generate && npm run build && node dist/main.js"

  web:
    build:
      context: ./web
      dockerfile: Dockerfile
    container_name: clinid_web
    depends_on:
      - api
    ports:
      - "3000:3000"
    environment:
      NEXT_PUBLIC_API_URL: http://127.0.0.1:3001
      NODE_ENV: development 
    volumes:
      - ./web:/usr/src/app
      - /usr/src/app/node_modules
    command: sh -c "npm install && npm run dev"

volumes:
  db_data:
